# ── 绝不入库：上游参考源码 ───────────────────────────────────────────
# _analysis/WorkDaddy 是 babygoton/WorkDaddy 的浅克隆（AGPL-3.0，自带 .git）。
# 提交它会造成两件事：(1) 形成嵌套仓库/gitlink 混乱；(2) 在本仓库内再分发上游代码。
# 任何人都不需要它来使用或开发本插件 —— 需要时请自行 clone 上游。
_analysis/

# ── 工作区内部资料 ─────────────────────────────────────────────────
# 助手工作区记忆，含本机环境细节与内部判断，不属于公开内容。
.workbuddy/

# 内部记录（宿主逆向分析的定位细节等），公开发布版本中已脱敏，细节仅留本地。
.internal/

# 内部代码审查报告。含对本项目与第三方上游项目的可利用性分析，
# 不适合随源码仓公开发布。如需公开审计结论，应另出一份移除利用细节的版本。
CODE-REVIEW-*.md

# ── 插件运行时产物与本地数据 ────────────────────────────────────────
# 备份目录含被快照的配置文件（可能带凭据），永不入库。
**/backup/
*.tmp-*
*.tmp

# ── 日志 ───────────────────────────────────────────────────────────
*.log
daemon.log

# ── Node ──────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
package-lock.json.bak

# ── 本地环境变量（凭据只存在于本机） ────────────────────────────────
.env
.env.*
!.env.example

# ── 编辑器与系统 ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
