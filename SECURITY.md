# 安全政策

## 报告漏洞

**请勿通过公开 issue 报告安全问题。**

请使用 GitHub 的私密报告通道：
**Security → Report a vulnerability**（仓库页面上方的 *Security* 标签页 → *Advisories* → *Report a vulnerability*）。

请尽量包含：受影响的文件与版本、复现步骤、影响判断（能读到什么、能改什么）、以及你建议的修复方向。若你不确定某个现象是否构成安全问题，也欢迎按此通道报告 —— 宁可多报。

**响应预期**：本项目为业余维护，不承诺固定时限。安全报告会被优先处理，修复发布会在此仓库说明。

---

## 支持范围

| 版本 | 状态 |
|---|---|
| `main` 分支 | 接受安全报告 |
| 已发布 tag | 仅最新 tag 接受报告 |
| pre-release（当前 P0 骨架） | 接受报告；**但请预期较大变动** |

---

## 安全设计约束

以下约束是设计的一部分，绕过它们即视为缺陷：

1. **零凭证外流** —— 传给渲染层的数据一律经白名单挑选。宿主或本地文件的原始对象不得跨 RPC 边界。
2. **凭据不落盘** —— 任何写入磁盘的数据在写入前须经凭据扫描。`settings.json` 含明文 API Key，**禁止原样复制**。
3. **不修改宿主** —— 不修改 `app.asar`、不重新签名、不重新打包官方安装包。
4. **不绕过认证** —— 不读取/上传宿主私有服务器协议，不迁移他人凭证。
5. **失败要可归因** —— 不得把配置损坏、权限不足等故障伪装成正常空态。

---

## 已知局限

这些是**已知且已评估**的局限，不是待报的漏洞：

### Windows 上文件权限保护失效

`fs.writeFileSync(..., { mode: 0o600 })` 与 `fs.chmodSync(file, 0o600)` **只对 POSIX 生效**。
Node 在 Windows 上不修改 ACL，因此这些参数是空操作，文件权限实际继承父目录的 DACL。

**处置**：本项目不依赖文件权限保护敏感内容 —— 凭据根本不进入备份（见上「凭据不落盘」）。
备份目录仅含经白名单裁剪的数据。若你需要更强的隔离，请自行收敛插件存储目录的 ACL。

### 插件进程的信任边界

插件运行在宿主提供的独立 utility process 中，通过官方 RPC 与视图通信。视图侧代码运行在宿主渲染进程中。
本项目**不**在视图与 Node 侧之间引入额外的认证层 —— 信任边界由宿主 pluginHost 定义，本项目按此边界设计。

### 权限按渠道分级

宿主会按插件来源渠道（随包 / 已安装 / 开发）过滤部分权限。请求被拒时本项目降级为"能力不可用"并如实上报，
不会尝试绕过。详见设计文档 §2.3。

### `QODER_APP_PLUGIN_ALLOW_UNVERIFIED`

开发加载流程需要在宿主环境变量中设置该开关，它会**关闭宿主对未签名源码的校验**。
这是宿主的开发通道行为，不是本项目的机制。**仅用于本地开发，验证完成后请立即清除该变量**
（操作步骤见 `qoderdaddy/README.md`）。

---

## 不在范围内

- 需要攻击者已经能读写你的本机文件系统、或已控制你的用户账户的攻击场景
- Qoder CN 宿主自身的漏洞（请报告给 Qoder 官方）
- 上游 [WorkDaddy](https://github.com/babygoton/WorkDaddy) 的漏洞（请报告给该项目）
- 仅影响外观、不影响数据或权限的问题
